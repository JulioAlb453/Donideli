/**
 * Último recurso si no hay sesión de admin o falla `GET /admins/contacto-chat`.
 * El identificador real debe ser el `nombre` del admin en BD (mismo que en login).
 */
export const ADMIN_COLLABORATION_USER_ID = '';
